{% extends 'base.html.twig' %}

{% block body %}
    <div class="m-4 row justify-content-center">
        {{ form_start(form) }}
        {{ form_widget(form, {'attr': {'class': 'center'}}) }}
        <div class="row justify-content-center">
            <button class="btn btn-primary btn-round">{{ button_label|default('Valider') }}</button>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}